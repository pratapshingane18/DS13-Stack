/*


( - Push into the stack
) - Pop from the stack

if(Something is left in the stack and expression is over ||

Stack is empty but expression is not over){

    Parenthesis is not proper;
}
    It only Check Parenthesis and not the expression
    
    */